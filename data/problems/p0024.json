{
  "id": 24,
  "title": "两两交换链表中的节点",
  "difficulty": "Medium",
  "tags": [
    "链表"
  ],
  "day": 3,
  "day_title": "链表",
  "category": "链表",
  "leetcode_url": "https://leetcode.cn/problems/swap-pairs/",
  "filename": "p024_swap_pairs.py",
  "doc": "🔹 题面\n    两两交换相邻节点(不许只换值)。\n\n🔹 解法对比\n    | 方法     | 时间 | 空间    | 备注          |\n    |----------|------|---------|---------------|\n    | 迭代 ★  | O(n) | O(1)    | 哨兵 + prev   |\n    | 递归     | O(n) | O(n) 栈 | 优雅           |",
  "solutions": [
    {
      "method": "swapPairs",
      "title": "迭代",
      "time": "O(n)",
      "space": "O(1)",
      "starred": true,
      "code": "def swapPairs(self, head):\n    dummy = ListNode(0, head)\n    prev = dummy\n    while prev.next and prev.next.next:\n        a, b = prev.next, prev.next.next\n        prev.next = b\n        a.next = b.next\n        b.next = a\n        prev = a\n    return dummy.next"
    },
    {
      "method": "swapPairs_rec",
      "title": "递归",
      "time": "O(n)",
      "space": "O(n)",
      "starred": false,
      "code": "def swapPairs_rec(self, head):\n    if not head or not head.next:\n        return head\n    nxt = head.next\n    head.next = self.swapPairs_rec(nxt.next)\n    nxt.next = head\n    return nxt"
    }
  ],
  "raw_source": "# LC 24. 两两交换链表中的节点 · Medium · 链表\n\"\"\"\n🔹 题面\n    两两交换相邻节点(不许只换值)。\n\n🔹 解法对比\n    | 方法     | 时间 | 空间    | 备注          |\n    |----------|------|---------|---------------|\n    | 迭代 ★  | O(n) | O(1)    | 哨兵 + prev   |\n    | 递归     | O(n) | O(n) 栈 | 优雅           |\n\"\"\"\nimport sys, os\nsys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))\nfrom _common.ds import ListNode, build_list, list_to_array\n\n\nclass Solution:\n    # 解法 1:迭代 ★ — O(n) / O(1)\n    def swapPairs(self, head):\n        dummy = ListNode(0, head)\n        prev = dummy\n        while prev.next and prev.next.next:\n            a, b = prev.next, prev.next.next\n            prev.next = b\n            a.next = b.next\n            b.next = a\n            prev = a\n        return dummy.next\n\n    # 解法 2:递归 — O(n) / O(n)\n    def swapPairs_rec(self, head):\n        if not head or not head.next:\n            return head\n        nxt = head.next\n        head.next = self.swapPairs_rec(nxt.next)\n        nxt.next = head\n        return nxt\n\n\ndef _test() -> None:\n    s = Solution()\n    cases = [([1, 2, 3, 4], [2, 1, 4, 3]), ([], []), ([1], [1]), ([1, 2, 3], [2, 1, 3])]\n    for arg, want in cases:\n        for fn in (s.swapPairs, s.swapPairs_rec):\n            assert list_to_array(fn(build_list(arg))) == want\n    print(\"✅ p024 通过 2 解法\")\n\n\nif __name__ == \"__main__\":\n    _test()\n",
  "viz": {
    "type": "list",
    "data": [
      1,
      2,
      3,
      4
    ],
    "label": "两两交换前"
  },
  "explanation": {
    "short": "**哨兵 + prev**:每轮取相邻 a, b,改 3 个 next 完成局部交换。",
    "long": [
      "**3 个 next 改写**:`prev.next = b; a.next = b.next; b.next = a`。",
      "**前进 prev**:本轮交换后 a 已是『二人组的尾部』,下次的『二人组前驱』就是 a。",
      "**循环条件**:`while prev.next and prev.next.next` —— 至少有 2 个待交换。",
      "**递归版**:`f(a) = let b = a.next; b.next = a; a.next = f(b.next)`,优雅但 O(n) 栈。"
    ],
    "complexity": {
      "time": "**O(n)**。",
      "space": "**O(1)** 迭代 / **O(n)** 递归栈。"
    }
  }
}