{
  "id": 208,
  "title": "实现 Trie(前缀树)",
  "difficulty": "Medium",
  "tags": [
    "字典树"
  ],
  "day": 5,
  "day_title": "图论 / 回溯 / 二分",
  "category": "图论",
  "leetcode_url": "https://leetcode.cn/problems/implement-trie-prefix-tree/",
  "filename": "p208_trie.py",
  "doc": "🔹 直觉\n    每节点用 dict 保存子节点;is_end 标记完整单词。\n    insert/search/startsWith 均 O(L)。",
  "solutions": [],
  "raw_source": "# LC 208. 实现 Trie(前缀树) · Medium · 字典树\n\"\"\"\n🔹 直觉\n    每节点用 dict 保存子节点;is_end 标记完整单词。\n    insert/search/startsWith 均 O(L)。\n\"\"\"\n\n\nclass Trie:\n    def __init__(self):\n        self.ch: dict = {}\n        self.end = False\n\n    def insert(self, word: str) -> None:\n        cur = self\n        for c in word:\n            if c not in cur.ch:\n                cur.ch[c] = Trie()\n            cur = cur.ch[c]\n        cur.end = True\n\n    def _walk(self, prefix: str):\n        cur = self\n        for c in prefix:\n            if c not in cur.ch:\n                return None\n            cur = cur.ch[c]\n        return cur\n\n    def search(self, word: str) -> bool:\n        n = self._walk(word)\n        return n is not None and n.end\n\n    def startsWith(self, prefix: str) -> bool:\n        return self._walk(prefix) is not None\n\n\ndef _test() -> None:\n    t = Trie()\n    t.insert(\"apple\")\n    assert t.search(\"apple\") is True\n    assert t.search(\"app\") is False\n    assert t.startsWith(\"app\") is True\n    t.insert(\"app\")\n    assert t.search(\"app\") is True\n    print(\"✅ p208 通过\")\n\n\nif __name__ == \"__main__\":\n    _test()\n",
  "viz": null,
  "explanation": {
    "short": "**Trie**:每节点用 dict 保存子节点,is_end 标记完整词。",
    "long": [
      "**节点结构**:`{children: dict, is_end: bool}`。",
      "**insert**:从根沿字符往下,缺失就创建子节点;末尾把 is_end = True。",
      "**search vs startsWith**:都是沿字符走;前者要求末尾节点 is_end,后者只要能走完。",
      "**空间优化**:固定字符集时改用 size 26 的数组(比 dict 快但常数稍多)。",
      "**变体**:LC 211 加 `.` 通配,需要 DFS 试遍所有子;LC 212 单词搜索 II 用 Trie 做剪枝枚举。"
    ],
    "complexity": {
      "time": "**O(L)** 每次 insert/search,L 为字符串长度。",
      "space": "**O(N · |Σ|)** 最坏 —— N 总字符数。"
    }
  },
  "modes": {
    "core": "# 题目要求设计前缀树,直接实现 Trie\n\nclass Trie:\n    def __init__(self):\n        self.children = {}\n        self.end = False\n\n    def insert(self, word):\n        node = self\n        for c in word:\n            if c not in node.children:\n                node.children[c] = Trie()\n            node = node.children[c]\n        node.end = True\n\n    def _find(self, word):\n        node = self\n        for c in word:\n            if c not in node.children:\n                return None\n            node = node.children[c]\n        return node\n\n    def search(self, word):\n        node = self._find(word)\n        return node is not None and node.end\n\n    def startsWith(self, prefix):\n        return self._find(prefix) is not None\n",
    "interview": "class Trie:\n    def __init__(self):\n        self.children = {}\n        self.end = False\n\n    def insert(self, word):\n        node = self\n        for c in word:\n            if c not in node.children:\n                node.children[c] = Trie()\n            node = node.children[c]\n        node.end = True\n\n    def _find(self, word):\n        node = self\n        for c in word:\n            if c not in node.children:\n                return None\n            node = node.children[c]\n        return node\n\n    def search(self, word):\n        node = self._find(word)\n        return node is not None and node.end\n\n    def startsWith(self, prefix):\n        return self._find(prefix) is not None\n\nif __name__ == \"__main__\":\n    t = Trie()\n    t.insert(\"apple\")\n    print(t.search(\"apple\"))      # True\n    print(t.search(\"app\"))        # False\n    print(t.startsWith(\"app\"))    # True\n    t.insert(\"app\")\n    print(t.search(\"app\"))        # True\n",
    "acm": "class Trie:\n    def __init__(self):\n        self.children = {}\n        self.end = False\n\n    def insert(self, word):\n        node = self\n        for c in word:\n            if c not in node.children:\n                node.children[c] = Trie()\n            node = node.children[c]\n        node.end = True\n\n    def _find(self, word):\n        node = self\n        for c in word:\n            if c not in node.children:\n                return None\n            node = node.children[c]\n        return node\n\n    def search(self, word):\n        node = self._find(word)\n        return node is not None and node.end\n\n    def startsWith(self, prefix):\n        return self._find(prefix) is not None\n\ndef main():\n    q = int(input())\n    t = Trie()\n    out = []\n    for _ in range(q):\n        op = input().split()\n        if op[0] == \"insert\":\n            t.insert(op[1])\n        elif op[0] == \"search\":\n            out.append(\"true\" if t.search(op[1]) else \"false\")\n        else:\n            out.append(\"true\" if t.startsWith(op[1]) else \"false\")\n    print(*out)\n\n\nif __name__ == \"__main__\":\n    main()\n",
    "acm_io": {
      "stdin": "第 1 行:操作数 q\n随后 q 行:`insert w` / `search w` / `startsWith p`",
      "stdout": "每个 search / startsWith 的结果(true / false,空格分隔)",
      "sample_in": "5\ninsert apple\nsearch apple\nsearch app\nstartsWith app\ninsert app",
      "sample_out": "true false true"
    }
  }
}